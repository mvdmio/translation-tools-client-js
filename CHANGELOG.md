# Changelog

## 2026-09-16: Translation Tools Client for Node.js

A Node.js project can install the Translation Tools Client and the Translation Tools CLI from npm. Developers edit JSON resource files, then generate typed keys and pull or push with TranslationTools. After initialize, the app looks up translations with bundled fallback and runtime refresh.
