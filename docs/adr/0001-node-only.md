# This client runs on Node.js only

JavaScript translation libraries often target the browser. This client is a Translation Tools Client for Node.js servers and build tools.

We do not ship a browser build, DOM adapters, or other JS runtimes (React Native, Electron, Deno).

The first consumers are Node processes. A browser library is a different product with a different bundle, locale source, and lifecycle.
